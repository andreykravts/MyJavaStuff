import java.util.Scanner;
public class ex1 {

	public static void main(String[] args) {
	/*Scanner input = new Scanner(System.in);
	System.out.println("Enter a char :");
char ch = input.next().charAt(0);
System.out.println("the ascii code is : "+ (int)ch);*/
		/*Scanner input = new Scanner(System.in);
		System.out.println("Please enter a number: ");
		int a,b,c;
		int num = input.nextInt();
		a = num%10;
		b = num/10%10;
		c= num/10/10;
		System.out.println("the result is "+(a+b+c));*/
		/*Scanner input = new Scanner(System.in);
		int a,b,c;
		int num = input.nextInt();
		a = num%10;
		b = num/10%10;
		c= num/10/10;
		System.out.println("the result is "+(c+b+a));*/
		/*Scanner input = new Scanner(System.in);
		int a,b,c;
		int num = input.nextInt();
		a = num%10; //אחדות
		b = num/10%10;//עשרות
		c= num/10/10;//מאות
		System.out.println("the result is "+(a*100+b*10+c));*/
		/*Scanner input = new Scanner(System.in);
		final int num = input.nextInt();
		int Restdays,weeks,months;
		months = num % 30;
		weeks = num %30 / 7;
		Restdays = num % 30 % 7 ;
		System.out.println("total of days"+num+" months :"+months+ "weeks :"+weeks+ "RestDays :"+Restdays);
				*/
		/*Scanner input = new Scanner(System.in);
		System.out.println("enter a num please : ");
		double  num, num1;
		 num = input.nextDouble();
		 num1 = input.nextDouble();
		double res = Math.sqrt(num);
		double abs = Math.abs(num);
		double pow = Math.pow(num, 3);
		double pow05 = Math.pow(num, 1.0/2);//שורש מהמספר
		double round = Math.round(num);//מעגל למעלה
		double floor = Math.floor(num);// צעגל למטה
		double max = Math.max(num, num1);
		double random = Math.random();
		double random10 = (Math.random()*10);
		double random10int =(int)Math.random()*10;
		int random5AND3 = (int)(Math.random()*5)+3;*/
		/*INT RANGE = MAX - MIN + 1;
		INT NUM = (INT)(MATH.RANDOM()*RANGE)+MIN;
		50 - 20 מספר בין 20 ו50
		20...75 range 75 -20 +1 = 56
		*/
		
		
	}

}
